[project]
name = "razrevrec"
version = "0.1.0"
description = "Adaptive revenue recovery engine for failed SaaS subscription payments"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

